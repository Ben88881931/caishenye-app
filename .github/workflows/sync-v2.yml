name: 自动同步 v2 数据

on:
  push:
    branches: [main]
  schedule:
    - cron: '30 14 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 生成 v2 数据
        run: python v2/sync.py

      - name: 有变化则提交
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git diff --quiet v2/data.js; then
            echo "v2/data.js 无变化，跳过提交"
          else
            git add v2/data.js
            git commit -m "自动同步 v2 数据到最新期"
            git push
          fi
